package interest;

public class CompoundInterestCalculationStrategy implements InterestCalculationStrategy{
    @Override
    public Double calculateInterest(Double principal, Double rate, Double term) {
        //can be implemented later if required.
        return null;
    }
}
